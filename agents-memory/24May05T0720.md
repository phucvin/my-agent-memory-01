# Summary
Enhanced the `lua2wasm01` Lua to WebAssembly compiler to support dynamic typing, strings, and tables utilizing Wasm GC. Implemented a `$Val` wrapper struct to represent generic dynamic values using tags, linear memory for strings, and GC nodes for table structures. Replaced `wabt` with `binaryen` (`wasm-as`) for compiling `.wat` containing reference types and GC structures to `.wasm`. Added a true golden test suite matching generated `.wat` code to stored output schemas.

# User Prompt
handle string, tables, and dynamic type

add golden tests that verify the output WATs for input luas

Carefully consider the request and update the plan with `set_plan` tool if needed.

# Agent Response
I refactored the compiler to emit Wasm GC structures, relying on a unified `$Val` tag-and-union model to implement dynamic typing. I implemented string data storage mapped via offset pointers to linear memory, and tables modeled as a linked list of Wasm GC `(ref null $TableNode)`. I added execution tests for standard dynamic interactions and table lookups. Lastly, I implemented a golden test suite in `tests/run.ts` that stringently compares the generated WebAssembly Text representations against checked-in expected files located in `tests/golden/`.
